import {
    DEFAULT_THEME, LIGHT_THEME,
} from './constants';

export const theme = {};
theme[DEFAULT_THEME] = {
    container: {
        backgroundColor: '#242B4A',
    },
};
theme[LIGHT_THEME] = {
    container: {
        backgroundColor: '#242B4A',
    },
};
